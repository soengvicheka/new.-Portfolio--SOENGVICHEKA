node_modules
dist
dist-ssr
*.local
.DS_Store
npm-debug.log*

# Local env files (secrets — never commit)
.env
.env.*
!.env.example

# Recovery utility output (private photo/CV copies — never commit)
scripts/.recovered/
